# Database
DATABASE_URL="postgresql://postgres:aitrackr_dev@localhost:5432/aitrackr?schema=public"

# NextAuth
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET=""  # openssl rand -base64 32

# Google OAuth (https://console.cloud.google.com)
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# Stripe (https://dashboard.stripe.com/test/apikeys)
# Run `npx tsx scripts/setup-stripe.ts` to auto-create the product + prices
STRIPE_SECRET_KEY="sk_test_..."
STRIPE_PUBLISHABLE_KEY="pk_test_..."
STRIPE_WEBHOOK_SECRET="whsec_..."        # from Stripe dashboard → Webhooks
STRIPE_PRICE_ID_MONTHLY="price_..."      # $8/month  — created by setup-stripe.ts
STRIPE_PRICE_ID_YEARLY="price_..."       # $79/year  — created by setup-stripe.ts

# Resend Email (https://resend.com)
RESEND_API_KEY="re_..."
RESEND_FROM_EMAIL="noreply@aitrackr.io"

# Cron secret (openssl rand -base64 32)
CRON_SECRET=""

# App
NEXT_PUBLIC_APP_URL="http://localhost:3000"
NEXT_PUBLIC_EXTENSION_ID=""
